# Treat any keyring failure as "no password" -- dbus ServiceUnknown fires when no secret service is installed
# See: https://github.com/htkhiem/euphonica/pull/267
--- a/src/client/connection.rs
+++ b/src/client/connection.rs
@@ -441,12 +441,8 @@
                 }
                 // eprintln!("Successfully authenticated");
             }
-            Ok(None) => {
-                // eprintln!("No password was specified.");
-            }
-            Err(e) => {
-                return Err(dbg!(e));
-            }
+            // No password set, or keyring unavailable (e.g. no secret service running) -- proceed unauthenticated
+            Ok(None) | Err(_) => {}
         }

         // Doubles as a litmus test to see if we are authenticated.
